class Help {
  constructor(userId, title, content, progress, time) {
    this.userId = userId;
    this.title = title;
    this.content = content;
    this.progress = progress;
    this.time = time;
  }
}

export default Help;
